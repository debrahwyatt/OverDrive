using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fly : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

/*    void Flying(int cost)
    {
        //Is Flying Activated
        if (Input.GetKeyDown(KeyCode.F))
        {
            if (flying == false) trueFlight();
            else falseFlight();
        }

        // if (overDrive == true) trueFlight();
        // else falseFlight();

        //Mana Burn
        if (flying) ManaAdjust(-cost);

        //In-Object Functions
        void trueFlight()
        {
            flying = true;
            playerGravity = 0;
        }
        void falseFlight()
        {
            flying = false;
            playerGravity = gravity;
        }
    }*/
}
